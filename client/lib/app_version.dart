/// App version shown on the About screen.
///
/// KEEP IN SYNC with `version:` in `pubspec.yaml` — guarded by
/// `test/app_version_test.dart` (no package_info plugin needed for one
/// string).
const String appVersion = '0.1.1';
