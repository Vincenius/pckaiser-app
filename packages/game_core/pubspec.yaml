name: game_core
description: >-
  Pure Dart game logic for the PC Kaiser mobile clone. Shared verbatim by the
  Flutter client (local mode) and the Dart shelf server (online mode).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
